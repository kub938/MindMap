meta {
  name: create
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/v1/mindmap
  body: json
  auth: inherit
}

body:json {
  {
    "title": "새로운 mindmap"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
